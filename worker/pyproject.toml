[project]
name = "miko-ai-factory-worker"
version = "0.3.0"
description = "Cloudflare Python Worker orchestrator for Miko AI Video Factory"
requires-python = ">=3.13"

dependencies = [
    "huggingface_hub"
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
